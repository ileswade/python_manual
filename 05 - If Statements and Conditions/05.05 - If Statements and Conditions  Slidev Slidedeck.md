---
theme: academic
background: https://source.unsplash.com/collection/94734566/1920x1080
class: text-center
highlighter: shiki
lineNumbers: true
info: |
  ## Chapter 5: If Statements and Conditions

  Decision making in Python with if statements, comparison operators, logical operators, and nested conditionals.

  CPRG216 - Python Programming Course
drawings:
  persist: false
transition: slide-left
css: unocss
---

# If Statements and Conditions
## Chapter 5: Making Programs Smart

<div class="pt-12">
  <span @click="$slidev.nav.next" class="px-2 py-1 rounded cursor-pointer" hover="bg-white bg-opacity-10">
    Let's make programs that think and decide! <carbon:arrow-right class="inline"/>
  </span>
</div>

<div class="abs-br m-6 flex gap-2">
  <a href="https://github.com/slidevjs/slidev" target="_blank" alt="GitHub"
    class="text-xl slidev-icon-btn opacity-50 !border-none !hover:text-white">
    <carbon:logo-github />
  </a>
</div>

---
transition: fade-out
---

# Programs That Make Decisions! 🧠

Now we're getting to the **exciting part!** If statements allow programs to make decisions and respond differently based on conditions.

<v-clicks>

- 📦 **Code blocks** - understanding indentation and structure
- ⚖️ **Comparison operators** - testing values against each other
- 🔀 **If-else-elif** - multiple decision paths
- 🔗 **Logical operators** - combining conditions with and/or/not
- 🪺 **Nested if statements** - decisions within decisions

</v-clicks>

<br>

<v-click>

<div class="p-4 bg-blue-100 dark:bg-blue-900 rounded">
🎯 <strong>This is where programs start to feel "smart" and dynamic!</strong> No more linear execution - your programs can now think and choose what to do.
</div>

</v-click>

---

# Understanding Code Blocks 📦

Before diving into if statements, we need to understand **code blocks** - one of the most important concepts in Python.

<v-clicks>

### What is a Code Block?

A **code block** is a chunk of code that belongs together. In Python, code blocks are created using **indentation** (white space at the beginning of lines).

```python
if age > 17:
    print("You are an adult")          # IN the code block
    print("You can vote")              # IN the code block
print("Thank you")                     # OUTSIDE the code block
```

### The Anatomy of an If Statement

```python
if condition:              # If line ends with a colon (:)
    # Code block starts    # Everything indented is INSIDE
    statement1
    statement2
    # Code block ends
statement3                 # Not indented = OUTSIDE
```

</v-clicks>

---

# How Code Blocks Work 🔧

<v-clicks>

**How it works:**

1. **Python checks** if the condition is True
2. **If True**, it runs ALL the indented code (the code block)
3. **After the code block**, it continues with the next un-indented line
4. **If False**, it skips the entire code block and goes straight to the next un-indented line

### Example in Action

```python
temperature = 75

if temperature > 70:
    print("It's warm outside")       # IN the code block
    print("Wear light clothing")     # IN the code block
print("Have a nice day")             # OUTSIDE - always runs
```

**If temperature is 75:**
```
It's warm outside
Wear light clothing
Have a nice day
```

**If temperature is 60:**
```
Have a nice day
```

</v-clicks>

---

# White Space is CRITICAL ⚠️

The **exact number of spaces matters**. Python needs consistency.

<v-clicks>

```python
# ✅ This works - consistent indentation
if age > 17:
    print("Adult")
    print("Can vote")

# ❌ This BREAKS - inconsistent indentation
if age > 17:
    print("Adult")
      print("Can vote")    # ERROR! Different indentation
```

**Real-world problem:** If you copy code from someone who uses 3 spaces and you use 4 spaces, mixing them will cause errors!

**VS Code helps:** It usually converts tabs to 4 spaces automatically, keeping things consistent.

</v-clicks>

<v-click>

<div class="mt-4 p-4 bg-red-100 dark:bg-red-900 rounded">
⚠️ <strong>Remember:</strong> Code blocks aren't just for if statements! You'll see them with loops, functions, classes - understanding this now helps throughout your programming journey.
</div>

</v-click>

---

# Comparison Operators: Testing Values ⚖️

To make decisions, we need to compare values. Python provides several comparison operators:

<v-clicks>

| Operator | Meaning | Example | Result |
|----------|---------|---------|--------|
| `==` | Equal to | `5 == 5` | `True` |
| `!=` | Not equal to | `5 != 3` | `True` |
| `>` | Greater than | `7 > 3` | `True` |
| `<` | Less than | `3 < 7` | `True` |
| `>=` | Greater or equal | `5 >= 5` | `True` |
| `<=` | Less or equal | `4 <= 4` | `True` |

</v-clicks>

<v-click>

### Examples in Action

```python
age = 25

if age == 25:        # Equal to
    print("You are 25")

if age != 30:        # Not equal to
    print("You are not 30")

if age >= 18:        # Greater than or equal
    print("You are an adult")
```

</v-click>

---

# ⚠️ Common Mistake: = vs ==

<div class="text-center text-2xl mb-6">
This is the #1 mistake beginners make!
</div>

<v-clicks>

```python
# ❌ WRONG - This is assignment, not comparison
if age = 25:    # ERROR!
    print("Hello")

# ✅ CORRECT - This is comparison
if age == 25:
    print("Hello")
```

**Remember:**
- **Single equals `=`** assigns a value
- **Double equals `==`** compares values

</v-clicks>

<v-click>

<div class="mt-6 p-4 bg-yellow-100 dark:bg-yellow-900 rounded">
💡 <strong>Memory tip:</strong> "Equality needs to be checked TWICE, so use TWO equals signs!"
</div>

</v-click>

---

# Basic If Statements 🚦

The simplest if statement checks one condition:

<v-clicks>

```python
age = 20

if age >= 18:
    print("You are an adult")
    print("You can vote")
print("Thank you")  # Always runs
```

**How it works:**
1. Is `age >= 18` true? YES (20 >= 18)
2. Run the code block (both print statements)
3. Continue to next line (outside code block)

</v-clicks>

<v-click>

**If the condition is False:**

```python
age = 16

if age >= 18:
    print("You are an adult")     # SKIPPED
    print("You can vote")          # SKIPPED
print("Thank you")                 # RUNS (not in code block)
```

Output: `Thank you`

</v-click>

---

# If-Else Statements: Two Paths 🛤️

Often you want to do one thing if True, and something else if False. Use `else`:

<v-clicks>

```python
age = 16

if age >= 18:
    print("You are an adult")
else:
    print("You are a minor")
```

**How it works:**
- If condition is True → run the if block
- If condition is False → run the else block
- **One or the other will ALWAYS run** (never both, never neither)

</v-clicks>

<v-click>

### Another Example

```python
temperature = 65

if temperature > 70:
    print("It's warm")
    print("Wear shorts")
else:
    print("It's cool")
    print("Wear a jacket")
```

</v-click>

---

# If-Elif-Else: Multiple Choices 🛣️

What if you have more than two options? Use `elif` (else if):

<v-clicks>

```python
age = 70

if age >= 65:
    print("You are a senior")
elif age >= 18:
    print("You are an adult")
else:
    print("You are a minor")
```

**How it works:**
1. Check first condition (`age >= 65`) → TRUE → run that block and **STOP**
2. (If first was false) Check elif condition (`age >= 18`)
3. (If both were false) Run else block

**Important:** Only ONE block runs! Once a condition is True, the rest are skipped.

</v-clicks>

---

# Order Matters with Elif! ⚠️

<div class="grid grid-cols-2 gap-6">

<div>

<v-click>

### ❌ Wrong Order

```python
age = 25

if age >= 18:
    print("Adult")       # This runs at 25
elif age >= 65:
    print("Senior")      # Never reached!
```

**Problem:** 65 is also >= 18, so the second condition never gets checked.

</v-click>

</div>

<div>

<v-click>

### ✅ Correct Order

```python
age = 70

if age >= 65:
    print("Senior")      # Check most specific first
elif age >= 18:
    print("Adult")
else:
    print("Minor")
```

**Solution:** Check more specific conditions first!

</v-click>

</div>

</div>

<v-click>

<div class="mt-6 p-4 bg-green-100 dark:bg-green-900 rounded">
💡 <strong>Rule:</strong> Put the most specific conditions first, then work toward more general ones.
</div>

</v-click>

---

# Logical Operators: Combining Conditions 🔗

Sometimes you need to check multiple conditions at once. Python provides three logical operators:

<v-clicks>

### The AND Operator

**Both conditions must be True for the result to be True.**

```python
temperature = 80
has_money = True

if temperature > 70 and has_money:
    print("You can buy ice cream!")
```

**Truth Table for AND:**

| Condition 1 | Condition 2 | Result |
|-------------|-------------|--------|
| True | True | **True** |
| True | False | False |
| False | True | False |
| False | False | False |

**Real-world:** "If it's warm AND you have money, you can have ice cream"
- Need **BOTH** to be true

</v-clicks>

---

# The OR Operator 🔀

**At least ONE condition must be True for the result to be True.**

<v-clicks>

```python
weather = "rainy"
temperature = 95

if weather == "rainy" or temperature > 90:
    print("Bring an umbrella")
```

**Truth Table for OR:**

| Condition 1 | Condition 2 | Result |
|-------------|-------------|--------|
| True | True | **True** |
| True | False | **True** |
| False | True | **True** |
| False | False | False |

**Real-world:** "Bring umbrella if it's hot OR rainy"
- Can use umbrella for shade (hot) OR for rain
- You bring it if **EITHER** condition is true

</v-clicks>

---

# The NOT Operator ↩️

**Reverses the condition - makes True into False and False into True.**

<v-clicks>

```python
is_raining = False

if not is_raining:
    print("You don't need an umbrella")
```

**Truth Table for NOT:**

| Condition | Result |
|-----------|--------|
| True | **False** |
| False | **True** |

</v-clicks>

<v-click>

### Combining Logical Operators

```python
age = 25
has_license = True
has_car = False

if age >= 18 and has_license and has_car:
    print("You can drive your own car")
elif age >= 18 and has_license:
    print("You can drive, but need to borrow a car")
else:
    print("You cannot drive")
```

</v-click>

---

# Nested If Statements: Decisions Within Decisions 🪺

If statements can be inside other if statements! This is called **nesting**.

<v-clicks>

```python
age = 25
has_license = True

if age >= 18:
    print("You are an adult")
    if has_license:
        print("You can drive")
    else:
        print("You need a license to drive")
else:
    print("You are too young to drive")
```

**How it works:**
1. First checks `age >= 18`
2. If True, enters that code block
3. Inside, checks `has_license`
4. Each level of nesting adds one indentation level

</v-clicks>

---

# More Complex Nesting Example 🏗️

<v-clicks>

```python
temperature = 85
has_money = True
store_open = True

if temperature > 70:
    print("It's warm outside")
    if has_money:
        print("You have money")
        if store_open:
            print("You can buy ice cream!")
        else:
            print("Store is closed, sorry")
    else:
        print("You need money for ice cream")
else:
    print("Too cold for ice cream")
```

</v-clicks>

<v-click>

### When to Use Nesting

**Use nested if statements when:**
- You need to check a second condition ONLY if the first is true
- The logic naturally has levels of decision-making

**Caution:** Too much nesting makes code hard to read!

</v-click>

---

# Practical Example: Grade Calculator 🎓

<v-clicks>

```python
score = 85

if score >= 90:
    grade = "A"
    print("Excellent work!")
elif score >= 80:
    grade = "B"
    print("Good job!")
elif score >= 70:
    grade = "C"
    print("Satisfactory")
elif score >= 60:
    grade = "D"
    print("Needs improvement")
else:
    grade = "F"
    print("Please see instructor")

print(f"Your grade is: {grade}")
```

This demonstrates clean, logical decision-making with multiple paths.

</v-clicks>

---

# Practical Example: Login System 🔐

<v-clicks>

```python
username = input("Enter username: ")
password = input("Enter password: ")

if username == "admin" and password == "secret123":
    print("Login successful!")
    print("Welcome to the admin panel")
elif username == "user" and password == "password":
    print("Login successful!")
    print("Welcome to the user area")
else:
    print("Invalid username or password")
    print("Access denied")
```

This shows how `and` operator ensures both conditions must be met for access.

</v-clicks>

---

# Practical Example: Ticket Pricing 🎫

<v-clicks>

```python
age = int(input("Enter your age: "))
is_student = input("Are you a student? (yes/no): ")

if age < 12:
    price = 5.00
    ticket_type = "Child"
elif age >= 65:
    price = 7.00
    ticket_type = "Senior"
elif is_student == "yes":
    price = 8.00
    ticket_type = "Student"
else:
    price = 12.00
    ticket_type = "Adult"

print(f"{ticket_type} ticket: ${price}")
```

This demonstrates complex business logic with multiple criteria.

</v-clicks>

---

# The Power of Simple Building Blocks 🧱

<div class="text-center text-xl mb-6">
🎯 **This is where programming becomes powerful!**
</div>

<v-clicks>

Think of it like building with blocks:
- **Each if statement** is simple to understand
- **But combining them** creates sophisticated programs
- **The algorithm** (steps to solve a problem) is built by arranging these pieces

**Examples of what you can now build:**
- Login systems with multiple user types
- Grade calculators with detailed feedback
- Game logic that responds to player choices
- Form validation with complex rules
- Shopping cart pricing with discounts and taxes

</v-clicks>

<v-click>

<div class="mt-6 p-4 bg-purple-100 dark:bg-purple-900 rounded">
🚀 <strong>Getting familiar with if statements is critical</strong> - they appear everywhere in programming. Start simple, practice often, and gradually work up to more complex combinations.
</div>

</v-click>

---

# Key Takeaways 🎯

<div class="grid grid-cols-2 gap-4 text-sm">

<div>

<v-click>

### Core Concepts
✅ **Code blocks created by indentation** - consistent spacing critical
✅ **Comparison operators:** `==`, `!=`, `>`, `<`, `>=`, `<=`
✅ **If-else gives two options**, if-elif-else gives multiple
✅ **Only ONE block runs** in an if-elif-else chain

</v-click>

</div>

<div>

<v-click>

### Advanced Techniques
✅ **Order matters** with elif - check specific conditions first
✅ **Logical operators:** `and`, `or`, `not`
✅ **Nested if statements** create complex decision logic
✅ **Combine simple pieces** for sophisticated programs

</v-click>

</div>

</div>

<v-click>

### Critical Reminders
⚠️ **Use `==` for comparison, `=` for assignment**
⚠️ **Consistent indentation is mandatory**
⚠️ **Test conditions in logical order (specific to general)**

</v-click>

---

# Reflection Questions 🤔

<v-clicks>

1. **What creates a code block in Python?**

2. **What's the difference between `=` and `==`?**

3. **In an if-elif-else structure, how many blocks can run?**

4. **What does the `and` operator require to return True?**

5. **What does the `or` operator require to return True?**

6. **Why does order matter when using elif statements?**

7. **When should you use nested if statements?**

</v-clicks>

---

# Practice Exercises 💪

<v-clicks>

1. **Age Checker:** Ask for age and classify as child (<13), teenager (13-19), adult (20-64), or senior (65+)

2. **Password Strength:** Check if password is longer than 8 characters AND contains a number. Print "Strong" or "Weak"

3. **Temperature Advisor:** Ask for temperature and whether it's raining. Give clothing advice:
   - Hot (>75) and not raining: "Wear shorts"
   - Hot and raining: "Wear light rain jacket"
   - Cold and raining: "Wear warm rain jacket"
   - Cold and not raining: "Wear a sweater"

4. **Movie Ticket Calculator:** Ask for age and student status. Calculate price:
   - Under 12: $8, 12-64 and student: $10, 12-64 regular: $15, 65+: $10

5. **Grade Calculator:** Ask for test scores and calculate letter grade with feedback

</v-clicks>

---
layout: center
class: text-center
---

# Decision Making Mastered! 🎉

Your programs can now think, decide, and respond intelligently!

### You Now Know:
✅ **Code blocks and indentation** - the foundation of Python structure
✅ **Comparison operators** - testing values and conditions
✅ **If-elif-else chains** - handling multiple decision paths
✅ **Logical operators** - combining conditions with and/or/not
✅ **Nested decisions** - complex logic from simple building blocks

<div class="pt-8">
  <span class="px-2 py-1 rounded cursor-pointer" hover="bg-white bg-opacity-10">
    Ready for Chapter 6: Loops and Repetition? 🔄
  </span>
</div>

---
layout: end
---

# Thank You!

## Next: Chapter 6 - Loops

<div class="pt-12">
  <span class="text-sm opacity-75">
    © 2025 Iles W. Wade | Free for educational use<br>
    Connect: <a href="https://www.linkedin.com/in/ileswade" target="_blank">linkedin.com/in/ileswade</a>
  </span>
</div>